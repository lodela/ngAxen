import { ScrollEventDirectiveDirective } from './scroll-event-directive.directive';

describe('ScrollEventDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollEventDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
